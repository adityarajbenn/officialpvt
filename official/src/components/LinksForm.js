// src/components/LinksForm.js
import React from 'react';
import { Button } from '@mui/material';

const LinksForm = () => {
    return (
        <Button variant="contained" color="primary" sx={{ mt: 2 }}>
            Add More Links
        </Button>
    );
};

export default LinksForm;
